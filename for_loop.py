def loop_with_range():
    # interval = range(5)
    # interval = range(-1, 3)
    # interval = range(5, 1) # sinnlos
    # interval = range(1, 5, 2)
    interval = range(5, 1, -1) 
    for element in interval:
        print(element)

loop_with_range()